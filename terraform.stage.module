{
	"action": "{{ var "terraformAction" }}",
	"artifacts": [
	  {{ module "github.artifact.module" }}
	],
	"expectedArtifacts": [
       {{ module "produce_artifact.module" "displayName" "planfile" "artifactAccount" "embedded-artifact" "type" "embedded/base64" }}
	],
	"name": "{{ var "stageName" }}",
	"overrides": {
		"environment_name": "{{ var "env_name" }}"
	},
	"profile": "",
	"targets": [],
	"terraformVersion": "{{ var "terraformVersion" }}",
	"type": "terraform",
	"requisiteStageRefIds": [
        "{{ var "previousStage" }}"
    ],
    "refId": "{{ var "stageOrder" }}",
	"workspace": "{{ var "workspace" }}"
}