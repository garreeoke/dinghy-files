{
	"action": "{{ var "terraformAction" }}",
	"artifacts": [
	  {{ module "github.artifact.module" }}
	],
	"expectedArtifacts": [
       {{ module "produce_artifact.module" "artifactAccount" "embedded-artifact" "type" "embedded/base64" }}
	],
	"name": "{{ var "stageName" }}",
	"overrides": {
		"environment_name": "{{ var "env_name" }}"
	},
	"profile": "",
	"targets": [],
	"terraformVersion": "{{ var "terraformVersion" }}",
	"type": "terraform",
	"workspace": "{{ var "workspace" }}"
}